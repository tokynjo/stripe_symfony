{% extends 'base.html.twig' %}

{% block body %}
    {{ parent() }}
    <div class="container-fluid">
        <h4>Modifier produit</h4>

        {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Editer" class="btn btn-primary col-xs-12"/>
        {{ form_end(edit_form) }}

        {{ form_start(delete_form) }}
        <br><br><br>
        <input type="submit" value="Supprimer" class="btn btn-danger col-xs-12">
        {{ form_end(delete_form) }}

        </ul>
    </div>
{% endblock %}
